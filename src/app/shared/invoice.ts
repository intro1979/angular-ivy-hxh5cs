export class Invoice {
  $key: string;
  code: string;
  name: string;
  dispatch_date: Date;
  invoice_date: Date;
  inv: string;
  cartons: number;
  amount: number;
  payment_terms: string;
  instructions: string;
}
